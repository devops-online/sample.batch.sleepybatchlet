<server>

	<featureManager>
		<feature>batch-1.0</feature>
	</featureManager>

    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="80"
                  httpsPort="443"/>
</server>
