<server>

	<featureManager>
		<feature>batch-1.0</feature>
	</featureManager>

    <application name='batch' location='../SleepyBatchletSample-1.0.war' type='war' context-root='/batch'/>

    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="80"
                  httpsPort="443"/>
</server>
